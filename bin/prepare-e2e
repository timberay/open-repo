#!/usr/bin/env ruby
# frozen_string_literal: true

# Script to prepare the database for E2E tests.

ENV['RAILS_ENV'] ||= 'development'

puts "Preparing database for E2E tests (environment: #{ENV['RAILS_ENV']})..."
system("bin/rails db:prepare") || abort("db:prepare failed")
puts "Database prepared."
