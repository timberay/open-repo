# Project-scoped Codex defaults.
#
# Hooks are enabled by default in current Codex releases. Keep this file small:
# user-level model, provider, sandbox, and authentication choices belong in
# ~/.codex/config.toml.
